# Re-base step 4: implementation notes

One entry per place where the implementation of `2026-09-21-rebase-boxes-4-evaluation.md` had to choose, because a
rule was ambiguous, an interface was incomplete, or an expected value did not follow from the rules. Each entry: the
task, what the plan said, what was done, why. The reading that needs the least machinery was taken every time.

Tasks 1, 2, 3 and 8 were built first, on the branch `rebase-boxes-eval`, while plans 2 and 3 were being built on
`rebase-boxes`; Tasks 4 to 7 and 9 wait for their stages.

| # | Task | The plan said | What was done | Why |
|---|---|---|---|---|
| N1 | 1 | `estimate_cost` prices cache-creation tokens at 1.25 × input "after plan 2's provider task"; the test expects `annotate` at $1.07, of which $0.05 is cache creation. That task had not landed when Task 1 was built, and "this plan adds no second price list". | One term was added to the return line of `scry.costs.estimate_cost` (`cache_creation_input_tokens × input price × 1.25`), nothing else of plan 2's provider task (no `batch` argument, no `add_usage`, no new test in `tests/test_costs.py`). | Without it the expected value cannot be met except by pricing in `accounting.py`, which the plan forbids. Plan 2's task rewrites the same line with the same arithmetic: when the branches merge, take `rebase-boxes`' `costs.py`; Task 1's tests pass on either. |
| N2 | 1 | `per_video = per_frame × projection`; dollars round to 4 places and dollars per video to 2. | `per_frame = round(dollars / frames, 4)`; `per_video = round(dollars / frames × projection, 2)`, from the unrounded quotient. | Multiplying a rate already rounded to 4 places by 221 frames can move the cent. |
| N3 | 1 | Every stage in the manifest or in `stage_seconds` has a `by_stage` row; the fields of a row without usage are given only for `dollars`. | Such a row has `dollars 0.0`, `dollars_batch 0.0`, `usage None`, `model` as the manifest has it (usually `None`), and `seconds 0.0` when the runner did not time the stage (`decode`, which every derived manifest carries). Rows follow the manifest's order, then the timed stages the manifest lacks. | Nothing to choose between; written down so Task 7 and Task 9 know the shape. |
| N4 | 2 | Rule 3 puts "a key set by two axes raises" under expansion; the test that pins it is `test_bad_keys_are_refused_at_load`. | `load_matrix` makes the check, over every pair of axes; `expand` only unions. `Matrix.stages` is likewise put into `STAGE_ORDER` at load. | The product combines every value of one axis with every value of each other axis, so the same files fail either way; at load both readings of the test hold, and `--dry-run` loads before it expands. |
| N5 | 2 | `config_for` "loads `base_config` with `tomllib`"; `base_config` is optional and defaults to `scry.toml`. | A base config file that does not exist raises `FileNotFoundError`; it is not read as "all defaults", unlike `scry.config.load_config`. | A mistyped `base_config` would otherwise run a whole phase on defaults; review focus 5 wants a typo to fail before a cent is spent. |
| N6 | 3 | `execute(m, spec, root, identity, …)` takes the root, not a `Run`; "an exception stops this run: status failed". | `execute` calls `materialise` itself. A spec-hash mismatch (`ValueError`) and a symlinked cache (`RuntimeError`) are raised out of `execute` and `run_matrix`, as the first test expects; only an exception from a stage is recorded as `failed` and lets the matrix go on. A finished run is skipped before the cache is looked at. | The two refusals are mistakes to fix before spending; there is no run to mark. |
| N7 | 3 | `execute` runs the stages "with the run's `Config`"; the code identity is "stored in `evalrun.json` when the run starts". | `config.json`, `code` and `cold` are written once, by `materialise` (the config is validated before `make_subset` creates anything). `execute` reads the config back from `config.json`, so a resumed run uses the config it started with even if the base file has changed, and `code` is the commit the run started on (what Task 9's header reports). | One source for what a run was; `cold` would read false on every resume if it were recomputed. |
| N8 | 3 | "`status done` → skipped. Anything else → the whole stage list is called again with `resumed true`." | `resumed` becomes true when the reused `evalrun.json` has any status but `new` (`running` or `failed`); a directory that was materialised and never started is not a resume. `started` keeps its first value; `finished` is set only when the run is `done`; `error` is cleared when a run starts again. `RunOutcome.seconds` is the accumulated dict of `evalrun.json`. | A fresh run passes through `new` inside the same `execute` call, so `new` has to mean "nothing ran yet". |
| N9 | 3 | `resolve(stage)` returns a `(run, cfg)` callable; the pseudo-stage `ask` calls `run_questions(run, cfg, spec.span.questions)`, "imported lazily". `run_matrix(identity=None)`. | `ask` does not go through `resolve` or `stage_funcs`: `execute` imports `scry.evaluation.questions.run_questions` at call time (Task 5 supplies it; a test replaces it by monkeypatching that module). With `identity=None`, `run_matrix` calls `code_identity` on the package's own directory, not the working directory. `only` with a name no run has returns `[]`. | The third argument does not fit the two-argument stage signature; the commit that matters is that of the code that runs. |
| N10 | 8 | Task 8 "consumes scorecards and `METRICS` (Task 7)", and can be built "once the `METRICS` table of Task 7 is written down"; Task 7 itself waits for plans 2 and 3. | `src/scry/evaluation/scorecard.py` was created holding only the `METRICS` table, copied from Task 7's table; Task 7 adds `build_scorecard`, `write_scorecard` and `score_phase` to that file. | `compare.py` imports the table from where the plan puts it, so nothing moves when Task 7 lands. |
| N11 | 8 | Rule 2: "for every pair `i < j` of cards that both hold the metric … Cards are the same configuration when their `config_id` and phase are equal." `n_a`, `n_b` are not defined. | `same_config_diffs` pairs only cards whose phase and `config_id` are equal, and a pair without a common unit adds nothing; `compare_configs` calls it once for each side, over the common units. A card holds a metric when the metric is a key of its `units`. `n_a` and `n_b` count the side's cards that hold the metric: the cards that are averaged, and the numbers the floor is scaled by. | The second sentence of rule 2 is read as a condition on the pairs, so a mixed list can never produce a cross-configuration "noise" value. |
| N12 | 8 | The card is Task 7's; the shape of `noise_table`'s result, the order of `flips` and the fields of a `not comparable` row are not given. | Read of a card: `run.phase`, `run.config_id`, `run.repeat`, `run.span` (the span's name, a string), `run.frames` (`[first, last]`), `units`, `cost.per_frame`, `cost.per_video`. `noise_table` returns `{config_id: {metric: {"values", "max"}}}`, for the metrics some repeat holds. Rows come in `METRICS` order; flips in unit order (shorter keys first, then by text, so `2` precedes `10`). A `not comparable` row has `None` means, difference and floors; with a side absent it has `units 0` and every unit seen as `dropped`. Costs are means over the side's cards that have a value, rounded as dollars (4) and dollars per video (2). | Sets have no order and the report needs a stable one; the rest is the smallest shape that holds what the rules name. |
