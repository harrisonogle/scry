# Re-base step 3: implementation notes

One entry per place where the implementation of `2026-09-21-rebase-boxes-3-interpret-summarize-index-ask.md` had to
choose, because a rule was ambiguous, an interface was incomplete, an expected value did not follow from the rules, or
the plan differed from the code committed on the branch. Each entry: the task, what the plan said, what was done, why.
The reading that needs the least machinery was taken every time.

| # | Task | The plan said | What was done | Why |
|---|---|---|---|---|
| N1 | 1 | `tests/minirun.py` has `mini_run`, `mini_interpretations` and `call_text`. Task 4's `test_labels_flag_and_rerun` "writes Fixture M's annotations into the same directory" of a run made without labels. | `minirun.py` also exposes `write_annotations(run)`, the function `mini_run(labels=True)` itself calls. The annotation lines are written as JSON objects by hand (not through the `Annotation` model), as the plan asks. | The test needs the same four lines a second time; one function instead of a copy. |
| N2 | 4 | Rule 4: `cost_usd` is `estimate_cost(usage, model)`; D18: "this plan never passes a batch flag to `estimate_cost`". Ledger L52 (1), later: "one cost figure per stage, at the price actually paid"; the committed `annotate` stage passes `batch=cfg.model.mode == "batch"` (plan 2's note N1). | `interpret`, the one stage of this plan that can run in batch mode, does as `annotate` does: `cost_usd = estimate_cost(usage, provider.model, batch=cfg.model.mode == "batch")`. `summarize` never uses the batch path and `ask` is synchronous, so both pass no flag. `run_costs` (Task 10) follows the plan and recomputes every stage from `usage` at the synchronous price. | The committed code and L52 win over the plan. In synchronous mode every figure agrees; for a batch-mode run the stage's `cost_usd` is half of what `run_costs` and plan 4's cost summary show for it, which is the limit plan 2's N1 already records. |
| N3 | 4 | The stat `labels` is a bool; the rule does not say of what. | `labels = bool(run.load_annotations())`: true exactly when `Run.load_labels()` is not `None`. Records of a missing PNG carry no `model` and no `prompt_version` (no call was made), as rule 2a writes them. | It is the condition `load_labels` itself applies, without building the view a second time. |
| N4 | 5 | Rule 17: "this stage never uses the batch path". Nothing is said about the provider's client, which `run_with_batches` closes inside the event loop for `annotate` and `interpret` (plan 2's Task 8: the "Event loop is closed" tracebacks). | `run_summarize` runs its coroutine with `asyncio.run` and closes `provider.client` in a `finally` inside that loop, the same five lines as `run_with_batches`; it does not go through `run_with_batches`, whose collecting pass would answer every dependent call with `pending`. `meter` is a dict `{"usage", "calls", "invalid_refs"}`; the video node's refs are validated and counted like any other node's. | Without the close, every real `scry summarize` would end with the traceback plan 2 removed from the other model stages. |
| N5 | 6 | Rule 15: `readers.vlm` is `{"text", "readings"}` "or None"; a link's payload has `lines`; rule 8's `containers` are "the container names in record order". Rule 13 adds a link's lines with the model's readings "when every lifetime of the link has a non-empty one". | `readers.vlm` is `None` when the lifetime has no label or no model reading at all (no annotations, or group-only records); a lifetime whose every model reading is empty keeps `{"text": None, "readings": {"": n}}`. A link's `lines` are its own lines (OCR join first, then the model's), without repeats inside the link; the entry's text then skips any line already listed. "Every lifetime of the link" is every id `link_lines` reads (run boxes, key and value, record cells; not a record's header). Empty container names and apps are left out of `containers` and `apps`. An empty line is never written into an entry's text. | Each is the literal reading; the payload keeps what was read even when it is not index text. |
| N6 | 8 | Rule 6: "the hits are the `frame`-level ids of every ranking"; rule 2 has only the `frame` queries select the text and the ordinal; rule 3 keeps the vector ranking as one list over all levels. | Every lexical query selects the same six columns and only the `frame` family's rows are kept for collapsing; a frame id that only the vector ranking returned is looked up by id (at most `k` lookups, and only with an embedder configured), so it joins the runs like any other. A frame hit carries `collapsed`, `members` and `matched` with collapsing off too (`collapsed 1`). `scry search --no-collapse` passes `collapse=False`; without the flag the config decides. | One query shape; the hit shape plan 4 reads does not depend on a switch. |
| N7 | 9 | The five tool descriptions "are prompt text"; `TOOL_DEFS` is produced by `scry.ask`. Rule 9: `messages=messages`. Rule 5 reads the lifetime of every box of the frame. | The descriptions live in `scry.prompts.ask.TOOL_DESCRIPTIONS` beside `SYSTEM` (both copied out of the plan by a one-off command, not retyped, as were the `interpret` and `summarize` prompts); `scry.ask.TOOL_DEFS` joins them to the input schemas. Each `create` call is handed a shallow copy of the conversation (`list(messages)`), so a recorded call shows the conversation as it stood at that turn. `Tools` is built before the API client, so a run without `index.sqlite` fails before any client exists; `scry ask` prints that message and exits 1. `get_frame` looks a box's lifetime up directly: a box in no lifetime (which plan 1 rules out) is a tool error that goes back to the model, not a special case. | Prompt text in one place, versioned by `VERSION`; the copy is what makes `test_ask_loop…`'s check of the third call meaningful. |
| N8 | 10 | "`tests/test_annotate_stage.py`: delete plan 2's `test_stage_list`". The end-to-end tests run `summarize` "with the answers of Task 5's first test". | No test named `test_stage_list` exists on the branch and nothing else pinned the stage list, so nothing was deleted; `test_run_executes_stages_in_pipeline_order` pins `STAGES`. The answers of Task 5's first test moved into `tests/minirun.py` as `summarize_answers(kw)`, which `test_summarize.py` and `test_pipeline.py` both import. An unknown name in `--stages` raises `typer.BadParameter`, which exits with code 2 and names the valid stages, before the run directory is created. | The committed code wins; one copy of the answers; the usage error is the CLI library's own exit code 2. |
