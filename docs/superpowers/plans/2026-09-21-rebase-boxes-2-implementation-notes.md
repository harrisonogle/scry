# Re-base step 2: implementation notes

One entry per place where the implementation of `2026-09-21-rebase-boxes-2-annotate.md` (Tasks 1 to 11) had to choose,
because a rule was ambiguous, an interface was incomplete, or an expected value did not follow from the rules. Each
entry: the task, what the plan said, what was done, why. The reading that needs the least machinery was taken every
time. Task 15 (incremental annotation) is built ahead of the other evaluation switches, for arm A, as ledger L53 (4)
asks: N11 to N15. Tasks 12 to 14 and the deferred task are not built.

| # | Task | The plan said | What was done | Why |
|---|---|---|---|---|
| N1 | 9 | Rule 10 and `test_every_frame_arm_a_end_to_end`: the manifest holds `cost_usd`, `cost_usd_batch`, `cost_per_frame_usd`, `cost_per_frame_usd_batch`, `cost_per_call_usd`. Ledger L52 (1): "one cost figure per stage, at the price actually paid; no `cost_usd_batch` field anywhere". | The manifest holds `cost_usd`, `cost_per_frame_usd` and `cost_per_call_usd` only. `cost_usd = estimate_cost(usage, model, batch=cfg.model.mode == "batch")`: the batch price when the stage ran in batch mode, the synchronous price otherwise. The test asserts that `cost_usd_batch` is absent where the plan expected `0.004`. | L52 overrides the plan. "The price actually paid" was read as "the price of the mode the stage ran in"; it is also the only caller `estimate_cost`'s `batch` argument (Task 8) has. Two limits, not repaired: `usage` is cold-equivalent (D23), so a batch-mode run served from answers paid synchronously is still priced at the batch price; and plan 3's `run_costs` and plan 4's cost summary recompute from `usage` at the synchronous price and halve, so for a batch-mode run their figure is twice this one. In synchronous mode every figure agrees. |
| N2 | 2 | `tests/annotate_fixtures.py` has `mk`, `frame`, `write_run` and Fixtures E, S, T. | Three more one-line builders, `fb(n, boxes)`, `change(id, a, b, components)` and `life(id, text, refs)`, and the fixtures as functions (`fixture_e`, `fixture_s`, `fixture_t`, `record_a10`, `record_a11`) that return fresh records. `write_run` takes `FrameBoxes` records and writes into `tmp_path` itself. Fixture S is there as Task 2 says, although nothing in Tasks 1 to 11 reads it (Task 13 does). | The same records are needed by five test files, and Task 3's Fixture P needs the same change and lifetime builders; fresh records per call let a test alter one without touching another test. |
| N3 | 5 | "The paragraphs copied character for character from this task." | The seven paragraphs were copied out of the plan file by line number with a one-off command, not retyped; the module holds them as named constants (`ROLE`, `IMAGES`, `CONTAINERS`, `ASSIGN`, `LINKS`, `TEXTS`, `DESCRIPTION`, and `NO_TEXTS` for group-only). The prompt module was first written before its tests by mistake; it was set aside, the tests were seen failing, and it was put back unchanged. `prompt_version` applies the formula to any arm and raises nothing; `system_prompt`, `output_model`, `build_blocks` and `to_proposal` raise `ValueError` for an arm other than `"A"` (and the first two for `pane=True`), as the plan says. | A retyped prompt can differ by a character, and every paid answer is cached under its version. |
| N4 | 5 | `output_model` is "memoised so that equal arguments return the same class". | A module-level dict keyed by `(arm, transcribe, pane)`, not `functools.cache`. | `functools.cache` keys on how the arguments were passed, so `output_model("A", True)` and `output_model("A", True, False)` would be two classes with one name, and the stage test's `is` would depend on the call's spelling. |
| N5 | 6 | "`Targets: all boxes.` when the plan's targets are every box of a frame that has boxes." | Decided by `list(plan.targets) == [ids in reading order]`; no targets gives `Targets: none.` first. | `plan_calls` returns targets in reading order, so list equality is the whole test. |
| N6 | 7 | `test_repair_records_and_headers` names no containers, assign or unassigned, and expects counts without `unplaced`. | The case's proposal lists every target in `unassigned`. `test_every_target_ends_up_exactly_once` is one parametrised test over the five cases' builders. A kept `Missed` keeps its text as returned (not stripped). `REPAIR_KEYS` is defined as the plan lists it, the keys of later arms included; nothing reads it yet. | Rule 9 keeps listed targets uncounted, so the expected counts hold; stripping a kept text is not asked for. |
| N7 | 8 | "When `provider` has a `client` with an awaitable `close`, await it inside the loop." | `run_with_batches` calls `provider.client.close()` when it exists and awaits the result when it is awaitable, in a `finally`. After an API error the returned usage is the four keys at zero, not `{}` (the sum over attempts). | The least code that holds for the SDK's async client and for the fake clients, which have no `close`. |
| N8 | 9 | The `arm` and `pane` keys arrive with Tasks 12 and 14; rule 10 wants `arm` in the manifest "(`"A"` until Task 12)". | `stage.py` holds `ARM, PANE = "A", False` and passes them where the config keys will go. `calls` counts records, so a `missing_png` record counts as a call although none was made (the plan: "`calls` (records)"). An error record keeps the `label_clashes` of the overlay that was drawn for it. With `mode = "off"` the manifest entry is rewritten on every run (no up-to-date check: it costs nothing) and holds exactly the keys of rule 1. | The plan's words, read literally. |
| N9 | 10 | Rule 4 defines "in force at frame *f*" frame by frame; rule 6 and the tests give `frame(9) == FrameLabel()` for a frame that is not in `boxes.jsonl`; `Labels` is described by its methods only. | `build_labels` returns `Labels(annotations, frames, lifetimes)`, which computes everything once. The links in force are carried in one pass over the frames in order: a link that has left force is never looked at again. The second condition of "in force" (the source of the mapped box is still the member in *g*) is checked for the link's members that were targets, not for its header ids, as the rule is worded. `Labels.frame(f)` never raises: for a frame outside `boxes.jsonl` it gives the screen-level labels of the latest successful record at or before it, with `links == []`. One record per frame is assumed (one call per frame); were there two successful ones, the later in the file would be used. | A lifetime never resumes and a box's source only moves forward, so "in force" cannot come back once lost, and the single pass gives the rule's result frame by frame. The rest is the plan's words read literally. |
| N10 | 11 | Rule 3: "`container` is that of the lifetime's latest labelled box." Rule 4 maps every accepted link to lifetime ids; `build_labels(…, lifetimes=[])` is a tested call. | The container is read from the `BoxLabel` of the lifetime's last box, whose source is by rule 1 the latest box of the lifetime that a successful record labelled. An accepted link that names a box without a lifetime (only possible when `lifetimes.jsonl` is missing or partial) is not mapped to a `LifetimeLink`. Two relations are "equal" when the link rewritten with lifetime ids serialises to the same JSON (kind, joiner, the id lists and their nesting). | One lookup instead of a second search; without the guard the tested call with no lifetimes would fail on A10's pair. |
| N11 | 15 | A validator: `mode = "incremental"` with arm B or C raises "incremental annotation needs arm A or D" (D28); `test_incremental_mode` has three arm cases; rule 3 and D28 speak of arms A and D. | No validator and no arm cases: the `arm` key arrives with Task 12, which is not built (`stage.py` still holds `ARM = "A"`, N8). `test_incremental_mode` asserts that the mode loads, alone and with `[model] mode = "batch"`. **The extension point is the comment on `AnnotateConfig.mode` in `config.py`:** the task that adds `arm` adds the validator and the test's arm cases with it. Nothing else waits for arm D: `plan_calls`, `repair` and the join never see the arm, and the stage hands every arm the same `CallPlan`, so arm D is incremental as soon as Task 12's `build_blocks` writes its user turn (which carries arm A's targets line). | A validator over a key that does not exist is machinery for nothing. |
| N12 | 15 | Rule 1: every call uses the every-frame system prompt and version, given as `annotate-v1`. The brief for this task: the prompt must make an incremental call's job plain (label only the targets; every box is numbered, so a link may name any of them; describe the whole screen). | The committed version is `annotate-v2` (L53) and the tests say so. `src/scry/prompts/annotate.py` and `blocks.py` are not touched and the version is not bumped. The v2 text was read against the three points and says each: the images paragraph has "a numbered box around every piece of text" and "names the targets: the boxes you are asked to label"; `assign` and `texts` are "for every target box id"; the links paragraph has "A link must include at least one target; it may also include boxes that are not targets"; the containers, missed and description paragraphs speak of the screen and never of targets. The user turn lists every id and then the targets. | The plan wrote the base prompt for both modes (Task 5 rule 6, L52 (2)). Saying it again would edit a shared paragraph, so bump the version (D22) and strand every answer paid under v2, with no run showing a fault (spec §12). What the first live incremental run should be read for: `repair_counts.not_target` and `text_not_target` (the model labelled carried boxes anyway, which wastes the output tokens the mode exists to save), and a container list or description that covers only the targets' part of the screen. Either is one sentence at the end of the images paragraph and a new version. |
| N13 | 15 | Rule 2: "the stage's inputs gain `run.changes` and `run.lifetimes`". Rule 5: the manifest gains `skipped_frames`. | The inputs gain the two files in incremental mode only; every-frame mode keeps `[frames.jsonl, boxes.jsonl]`, so a finished every-frame run stays up to date. The two files are checked for existence after the up-to-date check and the `scry read` check; a `changes.jsonl` that exists but lacks a transition raises `plan_calls`' own `ValueError`. `skipped_frames = frames − calls` is written in both modes (0 when every frame is annotated); `calls` counts records (N8), so a `missing_png` frame is a call, not a skipped frame. The entry for `mode = "off"` is unchanged. No other manifest key is new: `mode` already names the mode, and `boxes` was already "boxes over the call frames". | An every-frame run reads nothing of `track`, so it should not rerun when `track` does. One key present in every mode is simpler to read than a key that comes and goes. |
| N14 | 15 | Rule 4: "what the join makes of it (Task 10, unchanged)". D30: a transient failure is retried by the next run. The brief: extend the join's "in force" rules, do not duplicate them. | `join.py` is not changed and nothing was added beside it: Task 10's rules were written for both kinds of record (Fixture T's `A11` is an incremental record), and `test_incremental_run` reads the stage's own output through `Run.load_labels()`: the links in force at frame 12, which got no call, and `relinked == 1`. The retry rule is the committed one (`transient_errors` in the stage's manifest entry) and does not know the mode; with independent calls a rerun finds every answered call in the call cache. No test was added for either: the plan names none. `answer_from(record, kw)` in `tests/test_annotate_stage.py` turns a stored fixture record into the model's answer; in the every-frame half of the test, frames 11 and 12 are answered with `A11`'s content, since only the call for frame 10 is compared. | The plan's words; the join and the retry rule were built for this. |
| N15 | 15 (rule 6) | `incremental_projection(boxes, changes, lifetimes)` takes its calls and targets from `plan_calls`; `metrics.targets` and `test_targets` are deleted; `report.py` passes the lifetimes it selects. | Done as written; the test's values did not change (4 calls, 1 without targets, 113 of 415, 0.2723, 28.25, 103.75; then 3, 0, 113, 37.67). The fixture's thirteen lifetimes are `L1 [1:b5]` and `L2`…`L13 [3:b1]`…`[3:b12]`. Two consequences, not repaired: `scry.metrics` now imports `scry.annotate.targets`, which runs the package's `__init__` and so imports the stage and the providers (still pure, no longer light to import; no cycle, since nothing under `annotate` imports `metrics`); and `plan_calls` raises when a later frame has no transition into it, so `scry report` on a run directory without `changes.jsonl` fails at the projection where it used to print one call. The report is of what `track` measured, so that run had nothing to report. | Moving `plan_calls` out of the package or making its `__init__` lazy is more machinery than the import costs. |
