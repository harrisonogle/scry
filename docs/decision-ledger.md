# Decision ledger

Executive decisions made while landing the visual-transcript pipeline without the repo owner present (session started 2026-09-13, owner AFK). Newest entries at the bottom. Each entry says what was decided, why, and what to do if you disagree.

| # | When | Decision | Why | If you disagree |
|---|---|---|---|---|
| L1 | 2026-09-13 | **Implementation language: Python ≥ 3.12 (3.14.6 on this machine), managed with `uv`.** Recorded in the design as D13. | The whole perception stack is Python-first on macOS: Apple Vision via PyObjC, RapidOCR via ONNX Runtime, PyAV, numpy/scipy, the Anthropic SDK, SQLite FTS5 + sqlite-vec. Every dependency resolved and ran here. .NET 10 and Swift 6.3 are installed but have no good OCR-challenger or Vision story respectively. | Design §3 D13 lists the rejected alternatives; the stage boundaries and JSONL data model are language-neutral, so a port is a rewrite of modules, not a redesign. |
| L2 | 2026-09-13 | **OCR baseline: Apple Vision (`VNRecognizeTextRequest`, accurate, language correction off). RapidOCR is the optional challenger.** (Design D12.) | Vision transcribed a synthetic terminal frame exactly in 0.3 s; RapidOCR's default models dropped spaces and a line. Windows.Media.Ocr is Windows-only. | The engine is a config key; a Windows or Linux adapter slots into the same interface (design §8.1, §20.8). |
| L3 | 2026-09-13 | **No OpenCV. Decode with PyAV; pixels with numpy + scipy.ndimage.** (Design D14.) | OpenCV's wheel and PyAV each bundle FFmpeg; loading both on macOS logs duplicate Objective-C class warnings that promise "mysterious crashes". scipy does everything Stage 1 needs at ~5 ms/frame. | Re-add `opencv-python-headless` only if PyAV is dropped for the ffmpeg CLI path. |
| L4 | 2026-09-13 | **One VLM provider in v1: Anthropic, default `claude-opus-5`, behind a provider interface.** (Design D15.) | No OpenAI/Gemini keys or ground truth to compare against; the Claude API skill guidance defaults to Opus 5 and says not to downgrade for cost without the owner's say-so. Sonnet 5 / Haiku 4.5 are config alternatives. | Change `model` in the config; add a provider module for another vendor. |
| L5 | 2026-09-13 | **Stage 0 (Gemini agentic outline) is optional and every consumer runs without it.** | Needs a second vendor's key; unverifiable here. | Design §6 records the switch; the Gemini call can be added behind it. |
| L6 | 2026-09-13 | **Design changes beyond the port** (owner allowed design edits): `t_settled` corrected to the first still frame (§7.3); §9.0 fragment join added; §14.2 concrete index/tokenizer choices; §19.6 dollar estimate; overlay must not enlarge the frame (§8.2). | Each fixes a concrete problem found while checking the design against the tools (Vision fragments lines differently frame to frame; 1440p sits exactly at Claude's 4,784-token cap). | All are marked in §24 and can be reverted individually. |
| L7 | 2026-09-13 | **Superpowers skills were fetched from GitHub into the session scratchpad and followed from there rather than installed as a plugin.** | The plugin is not installed and, at the time, `github.com` refused connections (a transient network fault on the machine, gone after a reboot; only `raw.githubusercontent.com` worked); installing a plugin would change your global Claude config and would not load into the running session anyway. | `claude plugin install superpowers@claude-plugins-official` installs it properly for future sessions. |
| L8 | 2026-09-13 | **Commits go straight to `main`.** | You asked for commits and said you own nothing further in this session; there is no PR workflow in the repo. The superpowers executing-plans skill prefers a branch, but your instruction takes precedence. | `git revert` or branch from the pre-session commit `1b47a94`. |
| L9 | 2026-09-13 | **v1 retrieval defaults to lexical only (FTS5 `unicode61` with identifier token characters, plus a `trigram` companion table, fused by reciprocal rank); embedders are opt-in (`fastembed` local ONNX, or a hosted embedder).** | At the time, Hugging Face's model CDNs (`cdn-lfs.hf.co`, `cas-server.xethub.hf.co`) were unreachable from this machine (a transient network fault, gone after a reboot), so `fastembed` could not download its model; no hosted-embedder key is configured. The default is worth revisiting now that the network works (see `docs/open-items.md`). The question set is dominated by exact-string questions, which lexical retrieval serves. Verified: quoted phrase queries such as `"--resource-group"` and `"RG1-KodeKloud-AKS"` hit with the token-character tokenizer; `KodeKloud` as a substring hits only through the trigram table. | Set `index.embedder` in the config to `fastembed` (after placing the model in its cache from another network) or a hosted provider; the vector table and RRF path are built regardless. |
| L10 | 2026-09-13 | **Design revision 3 adopts nearly every finding from the two reviews** (archived in `docs/reviews/2026-09-13-design-r2-review-{fork,fresh}.md`). Headline changes: no morphological opening in change detection (it erased typed text); settle machine fixed (flush, max-hold guard, in-place upgrade); unmasked churn ring buffer with per-pixel hysteresis, churn ticks and a deactivation rule; blink tracker for block cursors; two images per Stage 2c call; VLM-proposed rows replace the geometric fragment join; sidecar files so no stage writes another's output; refs as `frame:line`; Stage 5 parallel; embedder default `none`; effort `low` for perception stages. | Both reviewers verified their claims on the sample video; the findings were concrete and mostly overlapping. | §24 of the design lists every change with section references; each can be reverted individually. |
| L11 | 2026-09-13 | **One review suggestion was not adopted:** `t_change = tPrev` (time of the frame *before* the first changed frame). Kept `t_change = t` (the first frame that no longer shows the previous state). | The stable interval is half-open `[t_settled, t_change)`, so `t_change` must be the first time the state is *not* shown. `t_settled = tPrev` (the first still frame) *was* adopted, since a frame that is still relative to its predecessor means the predecessor already showed the state. | Change the two assignments in §7.3 and the corresponding fixture expectations. |
| L12 | 2026-09-13 | **Where the reviewers differed, the choice was:** fixed-window fallback of 8 steps per section (fresh) rather than 5 (fork); region-correspondence weights 0.5·J + 0.3·IoU + 0.1·app + 0.1·name (blend of both); label placement order right → left → above → below (fork's right-first, fresh's never-inside); glyph strip allows a leading *or trailing* token of ≤ 2 characters (union of both rules); short-line alignment clause at len ≤ 8 (fresh). | Each is a parameter or a tie-break with no evidence either way yet; §16 lists them for tuning. | Edit §16 / the config file. |
| L13 | 2026-09-13 | **Stage 2c and Stage 5 run at `effort: low`; Stage 6 at `medium`; the answering agent at `high`.** | Opus 5 thinks by default and thinking bills as output; transcription and per-pair interpretation are perception tasks that do not benefit from long reasoning, and the cost model would otherwise be off by 2–3×. Bake-off item §18.3(4) checks `low` vs `medium`. | Change `effort` per stage in the config. |
| L14 | 2026-09-13 | **Redaction of on-screen identifiers is an open question (§22 #12), not a v1 feature.** | Both reviewers noted the sample shows a real-looking subscription ID and tenant domain; v1 indexes whatever is on screen because R1 forbids altering text, and the owner's own queries need the exact strings. | Add an allow-list/redaction pass at Stage 7 input if the corpus will be shared. |
| L15 | 2026-09-13 | **Server-side refusal fallbacks are off by default** (`claude-api` skill guidance says to enable them for Opus 5 / Fable code). | Fallbacks require the beta messages endpoint, which does not offer the `parse` structured-output helper the pipeline relies on, and are rejected on the Batches API anyway; screen transcription is an unlikely refusal trigger. A refusal is recorded on the record (`error: "refusal"`) and the run continues. | Add `fallbacks: "default"` + the `server-side-fallback-2026-07-01` beta to the provider's sync path if refusals show up in `manifest.json` diagnostics. |
| L16 | 2026-09-13 | **Batch mode is two-phase:** the stage runs once in "collect" mode to gather cache misses, submits them as size-chunked Message Batches, fills the on-disk call cache from the results, then runs the stage again (all hits). | Keeps one code path per stage and makes batch results indistinguishable from synchronous ones downstream; resumable through `batches.json`. | Set `[model] mode = "sync"` (the default) to bypass it. |
| L17 | 2026-09-13 | **Stage 0 uses Gemini's `interactions.create` with `processing: "agentic"` (google-genai ≥ 2.23) and asks for JSON in the prompt; also accepts `--import` of an externally produced outline.** Disabled by default; not testable here (no Google key). | That is the documented agentic-video call shape as of 2026-09-13; structured-output parameters for interactions were not documented, so the JSON is parsed leniently. | Flip `[outline] enabled = true` with a `GEMINI_API_KEY`; or produce `outline.json` elsewhere and import it. |
| L18 | 2026-09-13 | **The answering agent is a manual tool loop over `client.messages.create` with `tool_choice` left at auto** (no forced tool use, no Tool Runner beta). | Fable-class models reject forced tool use; a manual loop keeps image tool results (frames) simple; the loop is ~40 lines. | Swap in the SDK tool runner if the loop grows hooks. |
| L19 | 2026-09-13 | **Test scope: unit tests over synthetic frames, hand-written records and fake clients only; the plan's manual smoke checks on the sample video are labelled as such and never run under pytest.** | Your instruction: no integration tests until you hand-create ground truth; not test-heavy. | Add `tests/integration/` later, gated on the ground-truth set. |
| L20 | 2026-09-13 | **Plan revision 2 adopts both plan reviews** (archived as `docs/reviews/2026-09-13-plan-r1-review-{fork,fresh}.md`). Both reviewers extracted every code block into a scratch project and ran the tests: 13–15 of 69 failed as written. Fixes folded in: PyAV `av.time_base` is an int (duration must divide); settle machine defers novelty decisions that rest only on unconfirmed blink candidates and corrects the buffered emission on confirmation; region correspondence uses text containment, not Jaccard; perception repair keeps every mark exactly once and breaks parent cycles; caret is a box everywhere; layout_conf occlusion covers the occluded window's panes and counts pane rows; transient API errors are not cached; one event loop per stage in batch mode; batch error attribute path; Vision test tolerant of a dropped hyphen; several fixture and expectation bugs. | Independent execution is stronger evidence than reading; every fix was verified by the reviewer who proposed it. | Each is a discrete edit in the plan, cross-referenced in design §24 revision 4. |
| L21 | 2026-09-13 | **Where the two reviewers differed on the block-cursor fix, the deferral rule (fork) was chosen over excluding pending candidates from novelty only (fresh).** | The pending-exclusion rule needs two toggles before it applies, so when the cursor is merged with the typed glyph at keystroke time the first OFF toggle still emits one spurious state per pause; deferral covers that case and emits a genuine one-off small change at most 0.7 s late with correct times. | Swap `_novel` in `settle.py` for the pending-exclusion variant (both are in the archived reviews). |
| L22 | 2026-09-13 | **Design revision 4 was made as a byproduct of the plan review** (list in §24) rather than a separate review cycle. | The changes are rule-level corrections the code exposed; re-reviewing the design for them would repeat the plan review. | §24 lists each; revert individually. |
| L23 | 2026-09-13 | **No Anthropic or Gemini credentials exist on this machine, so the model stages were verified only on their no-credentials error path**, not with live calls; nothing was spent. A synthetic two-frame run (`runs/dry`, git-ignored) exercised every stage: OCR, overlay, perception (2 recorded API errors, none cached), merge (OCR-only fallback regions), diff (1 transition), interpretation (error recorded), hierarchy (fallback, `segmentation_conf: low`), index (6 nodes) and search. | Live calls cost money and there was no key to use anyway; the error path is what an unattended run hits first. | Set `ANTHROPIC_API_KEY` (or `ant auth login`) and run `uv run vt run assets/create-aks-cluster-tutorial.mp4 --out runs/aks`; Stage 1–2b outputs already in `runs/aks` are reused. Expect ≈ $18–45 on Opus 5 for the sample (§19.6). |
| L24 | 2026-09-13 | **Observed on the dry run and recorded as design §22 #15 rather than changed:** Vision misread `C:` as `Ci` in one of two near-identical frames; fused text prefers OCR when readings disagree, so the typed rule produced no event. | Changing which reading wins needs ground truth; the harness question is now written down. | If it recurs on the real video, prefer the VLM reading for `confusable`/low-similarity lines in `Line.fused`. |
| L25 | 2026-09-13 | **Executed the plan by extracting its code blocks into the tree** (a small extractor script; one commit per task in plan order, tests run per task group), rather than retyping. Three deviations from the plan's blocks, each committed with tests: (1) `layout_conf`'s singleton cap counts pane rows; (2) after the first sample run, the snapshot clocks (max-hold, churn tick) were unified to one snapshot per M, churn deactivation redefined as a region disappearing, snapshot upgrade guarded by "no churn active", and caret lookup given a blinker history; (3) record invariants asserted in every settle fixture. The plan file carries a header note; its code blocks were not re-synced. | The plan's code was reviewed and executed by two reviewers; copying it verbatim was the highest-fidelity path, and the divergences are small and documented. | `git log --oneline` shows one commit per task; diff a plan block against its file for any of them. |
| L26 | 2026-09-13 | **Stage 1 numbers on the sample (first run, before the L25(2) fixes):** 223 frames emitted (192 settled, 31 unsettled snapshots) from 25,585 decoded frames in 4 min 44 s wall; OCR 36 s (0.16 s/frame, 13,375 lines, median 53 per frame); overlays 6.5 s with 125 label clashes (~1 % of labels). The sample's terminal cursor does not blink, so terminal frames have no caret signal. After the fixes (commits 442fab6, 228dbc2): 221 frames (192 settled), carets on 29 frames, and every record satisfies `t_change ≤ t_settled ≤ t_end` with an unbroken chain and no snapshots closer than M — the first run had violated all three. | Recorded so the next person has a baseline. | — |
| L27 | 2026-09-13 | **The 221-state count on the sample was verified as faithful, not low.** A one-off whole-video scan (not kept in the repo) looked for settled-but-unemitted screens: frames still relative to their predecessor yet different from the emitted state for ≥ S. It found 10 departures of 32–179 px (cursor cells, 1-px lines, a text-field cursor) and nothing text-sized. The long gaps are narration over a still screen (16 gaps > 10 s cover 270 of 852 s; the longest, 40 s, has zero changed frames), and the bursts of change inside three of them are the transition animations into those states. Typing collapses to one state per pause ≥ S, which is the intended granularity. | The owner asked whether 221 was suspiciously low; the answer needed evidence. | Lower `[stage1.settle] still_s` in `scry.toml` for finer typing states (cost scales linearly in model calls); ground truth (§18.1) decides the right value. |
| L28 | 2026-09-13 | **First live smoke run of the model stages** (`runs/smoke`, frames 145–155 of the sample derived with `scry subset`; sync mode, `claude-opus-5`, effort per L13). Contract held: 31 calls, no API errors, refusals, schema failures or invalid refs; Stage 6 produced 4 steps / 3 sections with `high` confidence; `scry ask` answered a typed-command question correctly in 3 calls with a frame citation and flagged the OCR/VLM disagreement itself. Data did not: the VLM's transcription matched the marks it named in 7 % of 606 rows but matched *some* OCR line in 86 %, so `agree_fraction` was 0.022, `rows_rejected` 192, OCR-only lines 54 %, every transition carried ~100 ops with regions "appearing" on a static page, the PowerShell window failed correspondence at T10 (0.259 < `corr_accept` 0.3) and no `typed` event fired. Cost $2.21; Stage 2c $0.146 per frame with 4,680 output tokens per call. | Viewing the overlay at native resolution showed the cause: 11-px magenta digits on a 50 %-alpha black backing are unreadable, so the model enumerated ids in its own reading order. The smoke existed to catch exactly this before the ≈ $40 full run. | — |
| L29 | 2026-09-13 | **Overlay labels are opaque tags: black 12-px digits on solid yellow with 2-px padding** (was magenta 11-px on translucent black; §8.2 said "10–12 px on a 50 %-alpha backing"). Same 11 frames re-run: named-mark match 85 % of 628 rows, `agree_fraction` 0.363, `rows_rejected` 165, OCR-only 43 %, PowerShell window matched across T9 (0.819), the taskbar-tooltip frame 151 folded as a transient, Stage 2c output 1,860 tokens per call (was 4,680), perception wall 62 s (was 209 s), run cost $1.30: Stage 2c $0.076 per 1080p frame, Stage 5 $0.042 per transition, Stage 6 $0.11 for the subset. `label_clashes` 7 of ~850 (was 1). Full-sample projection: ≈ $27 sync, ≈ $14 batch. Two residual findings are in `docs/open-items.md`: 135 of the 165 rejected rows are label/value or table rows whose column gap exceeds `row_gap_lines = 3` (an in-memory re-merge at 30 gives 58 rejected and `agree_fraction` 0.518), and the `az login` keystroke produced no `typed` event because OCR read a spurious space in the path on one frame and the fused text is the OCR reading on disagreement (§22 #15, second occurrence). | Legibility was verified by eye at native resolution before spending; the model reads the ids when it can see them. Opaque beats translucent because placement never covers text anyway (§8.2). | Colors and padding in `src/scry/overlay.py`, `font_size` in `scry.toml`; §8.2 records the change. |
| L30 | 2026-09-14 | **Overlay A/B on the smoke subset, informed by the set-of-mark literature: neither a per-mark coordinate list nor 16-px labels beats the 12-px opaque tags; both stay off.** Runs `runs/smoke-coords` (`[model] stage2c_mark_coords = true`, +920 input tokens per frame, $0.89) and `runs/smoke-16px` (`[overlay] font_size = 16`, $0.87), Stage 2c + merge only, against the L29 baseline. New §18.4 diagnostic `mark_match_fraction` (rows whose VLM text matches the OCR text of the marks they name at Levenshtein similarity ≥ 0.8; stricter than the ratio quoted in L29, which scores the same baseline 0.85): baseline 0.726, coordinates 0.736, 16 px 0.748; `agree_fraction` 0.363 / 0.399 / 0.385; `rows_rejected` 165 / 154 / 159; all within sampling noise (± 0.02 on ~630 rows). 16 px doubles `label_clashes` (7 → 14). Classifying the baseline's unmatched rows: 6 % (35) are genuine id errors, mostly duplicated labels (two 'Node pools' lines on one screen, the two lines of a title slide swapped); 22 % are reading differences where OCR includes an icon glyph or garbles text ('P Search', 'Ea storage', '[8] Node pools', 'inttooliction o azure kunerneras sorwice') and the VLM reads the clean text, which is what fusion is meant to surface. The `stage2c_mark_coords` flag is kept, default off. | The L29 legibility fix already removed the id-reading failure; the literature's remaining levers (OmniParser's per-id text, larger marks) target a problem this pipeline no longer has. The residual id errors are ambiguity between identical lines, which coordinates did not resolve either; per-pane crops (v2) are the literature's answer to density. | Flip the flag or `font_size`; both variant run directories keep their outputs and share the call cache. |
| L31 | 2026-09-20 | **Correspondence and the line diff operate on window-level units — every parent-null region plus every popup — and panes fold into their nearest ancestor unit, surviving only as labels** (`DiffOp.pane`, rendered for Stage 5 as `[pane: left navigation]`). Decided by the owner after seeing the smoke run split the same portal page into 8 panes in frame 150 and 3 in frame 155. Implemented as `FrameRecord.units/unit_of/unit_lines/unit_line_sources` (design §11.1, revision 5.5) and used by `correspond`, `diff`, `coalesce` (transients, rules, retrospective focus at unit level), `interpret`, `diagnostics.fragment_stability` and the Stage 6 state line; Stage 3, `frames.jsonl` and the index are untouched, and `pane` is optional so old transitions still load. Re-diffing the smoke frames (`runs/smoke-windiff`, no model calls; per transition kind / ops / appeared / disappeared, baseline → now): T1 unsettled 0 [r1] [r1..r6] → 0 [r1] [r1]; T2 single 0 [r1..r5] [r1] → 0 [r1, r5] [r1]; T3 single 43 [r5, r6] [r5] → 55 [] [r5]; T4 single 39 [r3, r7] [] → 18 [r3] []; T5 single 16 [r5, r6, r7] [r3, r6] → 17 [] [r3]; T6 transient_merged 20 → 23; T7 single 15 → 17; T8 single 43 [r5] [r3, r5, r6, r7, r9] → 62 [r5] []; T9 single 23 → 21. Every appeared/disappeared id left is real (title slide → browser + PowerShell, the Cloud Shell tooltip at T4/T5, the PowerShell window leaving at T3 and returning at T8); the taskbar tooltip still folds as a transient; T3–T9 match the browser window to itself at 0.70–0.85. Op counts rise where the baseline had hidden changed lines inside unmatched panes (T3, T8) and fall where pane re-splits had been diffed as churn (T4). No `typed` event at T9 either way: the PowerShell unit's two ops are the baseline's (a window-control glyph deleted, and the prompt modify whose `Users \msadmin` → `Users\msadmin` OCR whitespace fails the prefix test; open item). | Panes were the noise source and windows were stable in all 11 frames; matching what is stable and labelling with what is not keeps the pane names for Stage 5 without letting them drive the diff. Ordering the merged lines by raw y centre was **not** stable — a 3-px taller title-bar box in frame 155 moved the PowerShell title past the window controls on the same row and produced a delete/insert pair — so lines are banded into rows at half the unit's median line height first (the §11.2 rule); about one same-text delete/insert pair per transition remains (band-boundary jitter or a genuine move). | Revert the seven modules and the five tests; the unit definition is three methods in `schemas.py`. To diff panes again, make `units()` return every region with lines. A unit whose only line is rewritten and that grows still scores just under `corr_accept` (§11.1) — a pre-existing gap the fixtures now avoid with a persistent line. |
| L32 | 2026-09-20 | **Stage 4 gates its ops with Stage 1's pixel rule: on a near-static transition (≤ 5 % of the screen changed, `pixel_gate_max_fraction`) an op whose line box meets no changed-pixel component is dropped.** Decided by the owner after a read-only prototype measured, on the 11 smoke frames (`scry.detect.change_map`/`components` with the `[stage1.detect]` parameters over the emitted PNGs), how the L31 structural ops (`runs/smoke-windiff`) relate to changed pixels: on the five near-static transitions (0.02–0.28 % of the screen changed) 90 of 96 ops sat on lines with no changed pixel — OCR reading the same pixels differently — while all 6 ops on changed lines were the genuine changes; on the two window switches (≈ 25 %) about 40 % of ops were under pixels; on the two full-screen cuts (> 80 %) nothing line-level survives; on a scroll every line moves and the structural diff is what recovers identical moved lines, so the threshold confines the gate to near-static pairs. Implemented as `PixelSource`/`pixel_gate` in `src/scry/diff.py` (design §11.2, revision 5.7): every op is marked `under_change` (the new line for insert/modify, the old line for a delete; `null` for a line without a box, never vetoed), the transition records `pixels = {changed_fraction, components, vetoed}`, the gate runs inside `diff_pair` so transient and coalesced rebuilds are gated the same way and the §11.3–§11.4 rules see the gated ops, Stage 5's diff text ends with `Pixels changed: X% of the screen in N areas`, and the manifest gains `ops_vetoed` and `pixel_gated_transitions`. Re-diffing the smoke frames (`runs/smoke-pixelgate`, no model calls; per transition kind / changed % / ops before → after / vetoed): T1 unsettled 82.95 % 0 → 0; T2 single 88.39 % 0 → 0; T3 single 25.12 % 55 → 55; T4 single 0.04 % 18 → 3 (15); T5 single 0.28 % 17 → 2 (15); T6 transient_merged 0.06 % 23 → 4 (19); T7 single 0.08 % 17 → 5 (12); T8 single 24.73 % 62 → 62; T9 single 0.02 % 21 → 1 (20). 81 ops vetoed over 5 gated transitions, none trivial. The 15 ops that survive on the gated transitions are the 6 under changed pixels (the `DefaultWorkspace-…` line re-read under a real change at T6 and T7, the browser status-bar URL at T5, the PowerShell prompt at T9) and 9 on VLM-only rows with no box. T9 is now exactly one op in the PowerShell unit, `PS C:\Users \msadmin> -` → `PS C:\Users\msadmin> az login`; the `typed` rule still does not fire because the OCR whitespace (and the caret read as `-`) fail the prefix test — the separate open item, unchanged. One rule interaction surfaced and was closed the same day: at T4 the three surviving ops are no-box VLM-only inserts at the end of the browser unit's list, and with the jitter gone Rule 2 emitted an `output_appended` event for the portal page; on a gated transition Rule 2 now needs at least one trailing insert with `under_change` true (no-box inserts ride along in the event but are not evidence) and Rule 1 needs its modify under changed pixels — a modify can sit on a no-box line, because `_ys` carries the last boxed y forward and two adjacent VLM-only rows pair by y (T5 has one). Ungated transitions and transitions without pixel evidence keep the old rules. After the fix T4 has no event and every other transition is unchanged. | Pixels are the ground truth for "did this line change"; the structural diff is the ground truth for "what moved". Each is wrong where the other is right, and on the smoke data the threshold separates the two regimes with a wide margin (0.28 % against 25 %). | `pixel_gate_max_fraction = 0` in `scry.toml` turns the veto off (the marks and `pixels` stay); the change is `PixelSource`, `pixel_gate` and the `pixels` kwarg threaded through `coalesce.py`; `pixels`/`under_change` are optional so old transition files load. The rule guards are the two `is_gated` checks in `_typed_op` and `_output_ops`. |
| L33 | 2026-09-21 | **Stage 2c group-only mode and image scale were measured on the smoke subset and neither becomes a default; both stay as switches.** Group-only (`[model] stage2c_transcribe = false`, `runs/smoke-grouponly`): output tokens 1,860 → 1,032 per frame, Stage 2c $0.076 → $0.055 per frame, perception wall 62 → 45 s; 90 % of rows are the identical mark sets of the transcribing run, `rows_rejected` 165 → 153, units per frame identical, no errors or repairs; the structural diff over OCR-only lines carried more jitter (244 ops vs 213) before the L32 gate. Image scale in group-only mode (`[overlay] scale`; `runs/smoke-s067`, `runs/smoke-s050`): $0.043 and $0.035 per frame, but rows identical to full resolution fall to 87 % and 81 %, `rows_rejected` rises to 179 and 190, and the hover tooltip stops being reported as its own popup on two frames at 0.67 and one at 0.5, which starves the transient rule; the 12-px tags drawn on the scaled image stay legible, the UI text does not. | With no transcription the two 1080p images are just over half of Stage 2c's cost, so resolution was the next lever; it buys ≈ 2 ¢ per frame (≈ $4 on the sample) with grouping fidelity. Whether the transcription itself is worth $0.021 per frame is the §18.3 (3) ablation and depends on §22 #15: today the model's reading only flags disagreements and fills OCR misses. | Set the two keys; every variant's run directory is kept and shares the call cache. Stage 5's images (two or three full-resolution frames per transition, ≈ $0.034) are the untested next target. |
| L34 | 2026-09-21 | **Cross-platform OCR: `rapidocr` 3.9.2 with its default PP-OCRv6 small models is the engine to carry off macOS; the adapter moves from `rapidocr_onnxruntime` 1.4 to it, with per-word boxes and a geometric spacing guard.** Thirteen configurations on the 11 smoke frames in isolated environments (scratchpad `ocr-engines/`; agreement with the vision model is a proxy, not ground truth): Apple Vision 0.19 s per frame, exact agreement 0.556, 0 of 12 grey Properties labels, 6 of 6 toolbar buttons; rapidocr 1.4.4 default 0.97 s, 0.694, 11/12, 0/6, space-only errors 12 %; rapidocr 3.9.2 v6 small ≈ 1.65 s under contention, 0.798, 11/12, 6/6, space-only errors 3.5 %, every spaced probe string exact including `PS C:\Users\msadmin> az login`; v6 medium 0.824 but five times slower; PaddleOCR PP-OCRv5 0.810 at 20 s per frame; docTR 0.525; EasyOCR 0.649. Per-word boxes show ≈ 10 px gaps between words against 0–3 px inside a word, so spacing can be rebuilt from geometry as a guard. Not run on Linux; timings under contention; the default engine is still `vision` until the smoke is re-measured on Rapid boxes. | The owner does not expect Apple hardware in the final system, and L2's baseline choice rested on a synthetic frame and the old package, whose dropped spaces are the worst failure for a command-and-path index. | `[ocr] engine`; Vision stays available on macOS. |
| L35 | 2026-09-21 | **The smoke subset re-run on rapidocr 3.9 boxes (`runs/smoke-rapid`, transcribing Stage 2c, pixel gate on) beats the Vision baseline on every reading measure; the default engine is left to the owner (open item).** Vision → Rapid: OCR lines 979 → 1,209 (the grey labels and icon glyphs now boxed), OCR 0.19 → 1.33 s per frame; `mark_match_fraction` 0.726 → 0.920; lines where the readers disagree 19.5 % → 4.7 %; `agree_fraction` flat at 0.365 only because `rows_rejected` rose 165 → 241 — more boxes per visual line hit the `row_gap_lines = 3` cap — and an in-memory re-merge at 30 gives `rows_rejected` 79, `agree_fraction` 0.630 (Vision at 30: 0.518); gated diff ops 116 vs 132; Stage 2c output 1,860 → 2,693 tokens per frame (JSON only 11 % longer; the rest is thinking over 110 marks instead of 89), $0.83 → $1.07 for the subset. The `typed` event fired for the first time on real video: T9 `typed " az login"` on `PS C:\Users\msadmin> az login`, because Rapid read the prompt identically on both frames (no stray space, no cursor glyph); the text still includes the shell's ghosted `z login`, which is the §22 ghost-text question. The prompt line disagrees with the model's `a login` and is flagged. Space guard fired 22 times over 11 frames. | Direct measurement on the pipeline's own data, after L34's proxy comparison; Rapid removes the two OCR failures that produced most disagreements (icon glyphs read as text, missed low-contrast labels) and the whitespace jitter that blocked the typed rule. The cost is ≈ 1.1 s more OCR per frame and ≈ 29 % more Stage 2c output tokens from the extra marks. | `[ocr] engine = "rapid"` in `scry.toml`; raising `row_gap_lines` is the companion change. |
| L36 | 2026-09-21 | **The default OCR engine is RapidOCR: `[ocr] engine = "rapid"` in `scry.toml` and the `OcrConfig` default; Apple Vision stays available on macOS as `engine = "vision"`.** Decided by the owner after L34–L35; design D12, §8.1 and §16 updated (revision 6.0). | L35 measured it on the pipeline's own data: mark match 0.726 → 0.920, disagreeing lines 19.5 % → 4.7 %, the typed rule firing for the first time on real video, for +1.1 s OCR per frame and +29 % Stage 2c output tokens; the owner does not expect Apple hardware in the final system, so the cross-platform engine is the one to tune against. D12's baseline choice rested on a synthetic frame and the old package. | One line in `scry.toml`; the Vision adapter is unchanged and every Vision run directory (`runs/smoke`, `runs/smoke-pixelgate`, …) is kept. |
| L37 | 2026-09-21 | **The row horizontal-gap test is off: `[merge] row_gap_lines = 0` (config default 0) and `_row_plausible` skips the gap test at ≤ 0; the vertical half-line-height test stays.** Decided by the owner; design §9.0 and §16 updated. | The owner considers the cap a finicky heuristic: on the smoke it rejected 116 real label/value rows at 3, and the outcome swung from 0.365 to 0.630 agreement between 3 and 30, so any value is a guess the next engine or layout invalidates. Since L31 the diff runs per window-level unit, so a cross-pane join inside one window no longer harms the diff; and the rows-as-boxes experiment queued next (`docs/open-items.md`) may retire the row mechanism entirely. | Set `row_gap_lines` to a positive number of line heights and the test comes back unchanged; merge is free to re-run. |
| L38 | 2026-09-21 | **The pixel gate grows each changed-pixel component by half a line height before testing whether a line's box meets it: `[diff] pixel_gate_margin_lines = 0.5` × the unit's median line height (a fixed 8 px when the unit has no boxed lines; 0 = L32's exact intersection).** Decided by the owner; design §11.2 and §16 updated. Re-measured on `runs/smoke-rapid-margin` (the L35 frames, diff stage only): every transition's ops and vetoes are identical to `runs/smoke-rapid` (116 ops, 79 vetoed; T4 0/15, T5 1/11, T6 1/16, T7 1/16, T9 1/21) and T9's `typed " az login"` survives. Of the 90 exact-test misses on the gated pairs, 88 sit ≥ 102 px from any changed component (margin used: 10 px); the two within reach are the same tooltip fragment `ingress Not enabled` on the transient's intermediate singles 150→151 and 151→152, whose box starts at x = 1328 exactly where the component ends — OCR re-fragmenting a line as the tooltip appeared, so arguably jitter — and T6 is rebuilt from 150→152 where it does not occur. | Exact intersection is fragile by construction: the OCR box and the changed-pixel component come from different tools, and a keystroke's ink, a cursor or a descender can land a few pixels outside the box the engine drew around the previous text. On the smoke every real change already sat inside its box, so the margin is insurance whose measured cost is nil. | `pixel_gate_margin_lines = 0` restores L32's exact test; the diff is free to re-run. |
| L39 | 2026-09-21 | **Stage 5 sends both frames at half scale by default** (`[stage5] images = "scaled"`, `scale = 0.5`; modes `full`, `crops`, `text` remain selectable). Measured on the 9 smoke transitions (`runs/smoke-rapid-s5-*`, one run per mode, Opus 5 effort low): full $0.356, 4 invalid refs, mean confidence 0.84; scaled $0.169 (47 %), 3, 0.84; crops (full-resolution crops of the changed-pixel areas plus a half-scale context frame; falls back to scaled when more than 25 % of the screen changed, which happened on 3 of 9) $0.174 (49 %), 3, 0.83; text only $0.114 (32 %), 1, 0.57. Reading the actions side by side: scaled kept hovers, tooltip text, cursor occlusion and the window switch and lost only the greyed inline prediction on the keystroke; crops kept everything including the prediction and the cursor shape, with one tooltip clipped by the 40-px padding; text only fabricated a click, misplaced the cursor and explained a tooltip as an OCR artifact. | Half the cost for no measurable loss on this subset; the diff text carries the text changes and the images carry the non-text actions, which survive at half scale. Owner agreed. | `[stage5] images`; `crops` with a wider `crop_pad` is the option if keystroke-level detail matters. |
| L40 | 2026-09-21 | **Masking the screen text for the Stage 2c call is not adopted** (`[overlay] mask` stays `none`; the modes `opaque`, `opaque_label`, `rendered` remain for experiments). Owner's hypothesis: in group-only mode the model still reads the text, which costs thinking and ties grouping to legibility. Measured on the 11 smoke frames with Rapid boxes, group-only, full scale, one run each (`runs/smoke-mask-*`, $3.38): output tokens per frame baseline 1,342, opaque 1,621 (+21 %), opaque with the number inside the box 1,283, rendered OCR text inside white boxes 1,327, rendered at half scale 1,522; the JSON is ≈ 550 tokens in every mode, so 700–1,000 tokens of thinking remain whatever the model can read. Rows identical to the baseline 84–86 % at full scale, 68 % at half scale; both opaque modes fold the taskbar tooltip on frame 151 into the properties column it covers; rendered at full scale keeps it and also promotes the clipped Cloud Shell mark on 149 to a popup; rendered at half scale clips many lines because Menlo is wider than the UI font. | Hiding the text does not stop the model thinking about layout, and it removes the cue that separates an overlapping popup from what it covers; re-rendering only helps if it can be made to fit at small sizes. | The modes are one config key; a proportional font and a lower size floor would make `rendered` a fair test at reduced scale. |
| L41 | 2026-09-21 | **Stage 2c structural variants measured: windows-and-popups-only is rejected; rows-as-boxes with model-proposed associations stays a switch (`[model] stage2c_rows = "boxes"`), not the default; panes stay in the schema (`stage2c_panes = true`).** Baseline under the current defaults (Rapid boxes, gap test off, cached Stage 2c calls, `runs/smoke-base2`): `mark_match_fraction` 0.920, `agree_fraction` 0.752, disagreeing lines 13.9 %, `rows_rejected` 27, T9 typed event present, $1.07. Windows-only (`runs/smoke-nopanes`): −19 % output tokens, but told to put everything in a window's rows the model builds rows across the two Properties columns, the vertical check rejects 78 rows, agreement falls to 0.404, OCR-only lines quadruple, T9's typed event is lost and a spurious one appears at T4. Rows-as-boxes (`runs/smoke-boxes`): `rows_rejected` 0, agreement 0.768, disagreements 10.5 %, mark match 0.922, +30 % output tokens ($1.29), one line per mark (≈ 110 per frame); the associations on frame 150 pair 21 Properties rows and 7 Essentials rows correctly and `Resource group : RG1-KodeKloud-AKS` is a region hit in `scry search`; the repair pass still fires (6 unknown or duplicate ids); T9's typed event was blocked by a one-off `delete 'Monitoring'` in the browser unit through `_other_ops_ok`. Both together (`runs/smoke-nopanes-boxes`): agreement 0.776 and T9 present at $1.20, but it inherits the column over-merge and drops region nodes from 95 to 36. | Panes evidently help the model keep rows geometrically sane even though they are only labels downstream. With the gap test off, rows-as-boxes gains little agreement for +20 % cost; its structural benefits — no row geometry, explicit searchable associations — are real but change what a line means for the diff and the index, which the harness should judge. One sample per variant. | Set the two keys; the `_other_ops_ok` sensitivity and the empty-transcription policy are open items. |
| L42 | 2026-09-21 | **The per-mark coordinate list helps grouping at reduced scale but the combination does not beat full resolution on cost, so Stage 2c stays at scale 1.0 with `stage2c_mark_coords` off.** Three repeats per condition at `[overlay] scale = 0.67`, group-only, Rapid boxes, each run in its own cache-free directory (`runs/smoke-c67-{off,on}-{1,2,3}`, $3.14 total), scored against the full-resolution group-only run (`runs/smoke-mask-none`): without coordinates rows identical to full resolution 70–75 %, `rows_rejected` (vertical test only) 75–95, repairs 5–13, unassigned marks 4–14, the Cloud Shell tooltip on frame 149 lost in one run of three, $0.43–0.48 per subset; with coordinates 74–86 %, 25–41, 1–2, 1, both tooltips kept in all three runs, $0.52–0.70. Full resolution without coordinates costs $0.63 for the same subset with 100 % fidelity by definition, so 0.67 plus coordinates costs about the same for less. The repeats also measure run-to-run noise for the first time: repairs vary 5–13 and one tooltip appears and disappears across identical runs, so single-run differences of a few percent in the earlier ledger rows (L30, L33, L40, L41) are within noise. | The coordinates give the model the line geometry it cannot read at reduced scale, which fixes rows spanning lines and keeps small popups separate; the text they add costs as much as the pixels they replace. | Set `stage2c_mark_coords = true` with a reduced `scale` if a cheaper Stage 2c ever matters more than fidelity; at full resolution the list was neutral (L30). |
| L43 | 2026-09-21 | **RapidOCR runs with the orientation classifier off (`Global.use_cls = false`) and one line per recognition batch (`Rec.rec_batch_num = 1`). No second OCR pass, upscaling, photometric normalisation, English model or OCR ensemble is adopted.** Measured against hand-built ground truth for 107 prompt rows of the second span, frames 155–187 (`scratchpad/ocr-second-pass/`, `ocr-ensemble/`, `ghost-text/`; no per-video constants in anything adopted). The baseline reads settled text well (scrollback prompt rows 52/53 exact, 76/77 sightings) and live command lines badly (8/16; 3/11 when PSReadLine's grey predicted text is on the line); every misread sits at the cursor cell, where a bright underline bar overlaps a dim glyph (l read as 1 or _, a space lost), or is a dropped space on a line carrying predicted text; normalisation makes the dim text legible and fixes nothing. `use_cls = false` changed 116 of 3,964 lines, 112 of them low-confidence garbage that became correct text, and no prompt row. `rec_batch_num = 1` removed the `SubscriptionID`/`Subscription ID` flip on unchanged pixels (L35 residue), changed 113 lines of which 37 gained a space and none lost one, and ran about 40 % faster. Rejected with numbers: re-recognising each box (best honest variant 7/16 live, 8 good rows broken; the one variant that helped needs a 2–3 px trim that breaks rows at 1 or 4 px), 2x/3x upscale (4/16 and 2/16), gamma, equalisation and Otsu (break 12–65 rows), the English PP-OCRv5 model (5/16, 20 broken), v6 medium (6/16, 7 broken, 4x slower), EasyOCR (0 exact), Google Cloud Vision alone (7/16 live, 32/53 scrollback; it misreads `group=RG1-KodeKloud-AKS` on all 21 sightings and merges a neighbouring pane unless clipped to the window). OCR confidence does not flag the bad lines (misreads score 0.96–0.99). What does: disagreement with the vision-language model's reading, 9/9 misreads with 10 false alarms in 71 (4 in 71 when Cloud Vision must disagree too). The model is the best single reader of live lines (12/16) and worse on scrollback (48/53, off-by-one row assignments); the best ensemble (character vote of Rapid, Vision and the model) reaches 12/16 live and 53/53 scrollback and is never right when no member is. Five of the live lines showed predicted commands that were never run; every executed command reads correctly from the majority of its scrollback sightings (7/7), against 4/7 from its first sighting. | The two settings are engine hygiene with no tuning. Everything else either breaks settled text or needs to know where the cursor or the predicted text is, which the owner ruled out. The findings argue for keeping the model's second reading as the disagreement flag, and for recording a text's majority reading over its sightings rather than its first; both go to the re-base proposal, not into this change. | Both are parameters in `src/scry/ocr/rapid.py`; Cloud Vision stays available as an optional third reader (the `GOOGLE_API_KEY` in `.env` works). |
| L44 | 2026-09-21 | **The owner is away from here until the re-base lands; this row is the mandate, and every decision made under it gets its own row below.** Reconcile the four build plans and their read-only reviews, implement the re-base on branch `rebase-boxes` (proposal `docs/proposals/2026-09-21-boxes-mode-rebase.md`, plans under `docs/superpowers/plans/`), run the free phase P0, then the paid phases; if that lands, bring back the optional Gemini outline stage (the old Stage 0, `outline` in the new names) and exercise it live; after that, run sweeps, choosing each next step from the last result rather than from a fixed sequence. Budgets, pre-approved in full and prepaid: Anthropic (≈ $470 left of $491), Google (≈ $250, shared by the Cloud Vision and Gemini keys). Standing rules: parallelise; no drafter or reviewer prototypes; simplify before repairing and do not add machinery pre-emptively, and even with evidence ask whether an addition overfits the sample video; no per-video constants; nothing findable today becomes unfindable and nothing is dropped without a stated reason; do not over-weigh tests or validation methodology where they would force a worse design; what matters is that it works. Defaults chosen without an answer from the owner: nothing is pushed; nothing is merged to `main` (tag `pre-rebase-boxes`) until the owner has looked. | The owner's instruction of 2026-09-21. | Everything is on the branch; `git checkout main` is the untouched pre-re-base state. |
| L45 | 2026-09-21 | **Build plan 1 (`read`, `track`, the free phase P0) reconciled in place with its two read-only reviews, both "accept with changes", under the mandate of L44. The rule applied was the proposal's §12, simplify before repairing.** (1) `track` is simplified: no groups, no connected components, no pools. Untouched boxes first match untouched boxes at the same place and are unchanged; equal texts at the same place, then equal texts anywhere among touched boxes (moves), drop out; every remaining box then pairs one to one, greatest intersection first with ties by reading order, with a box of the other frame, touched or not, provided at least one of the two is touched; a touched box left alone has appeared or been removed; an untouched box left alone is flicker, counted, with no record. A change record is one such pair, or one appeared or removed box, with exact strings, a character diff of that pair only and a whitespace-blind kind; `continues` names the previous transition's record whose later box is this record's earlier box, whatever its kind; only a `reread` continues a lifetime. The coordinator's pairing rule was worded for later boxes; it is applied in both directions by one candidate rule, so a shrinking text is found the same way as a growing one. This replaces the plan's D8 and D12 and dissolves fork S1 (sliver overlaps between adjacent lines chaining them into one group), fork S2 (a pulled-in untouched box surfacing as appeared or removed), fork S7 (`continues` undefined after an `appeared`), fork N1 and the first point of fresh S3 (reading order deciding a joined text) and fresh S4 (a quadratic character diff over a giant group). (2) Dropped everywhere: the clock regex, the `clock` field and the `trivial` transition kind (owner; this also dissolves fresh S2), and the `confusable` flag (owner; fork S4). Empty-text boxes stay dropped and counted. (3) The incremental-annotation projection follows the amended proposal (fork S3, fresh B1): a labelling call for the first frame and for every transition with a changed component or without pixel data; the targets are the boxes whose lifetime starts at that frame, a `reread` not among them; it reports calls, calls without targets, target boxes, share of boxes and targets per call, since the saving is boxes per call and not calls. The test's values were recomputed by hand: 4 calls, 113 target boxes of 415, 0.2723, 28.25 per call. (4) So that P0 can read H3 and H1 (fresh B2): moved pairs are persisted as box-ref pairs and listed in a `## Moves` report section; a lifetime's readings carry the frames of every sighting, and its evidence sheet shows each reading at the first frame where it was seen. Nothing beyond that. (5) Task 1 gains a "Returns with" table (fork S5, fresh S6) naming every capability deleted or reduced on the branch and the plan that restores it from tag `pre-rebase-boxes`: the labelling call, its overlay and the batch path, `agreement` and `mark_match` (plan 2); interpretation with its half-scale default of L39, summarisation, the index build, `search`, the agent and its prompt, the full `run` wiring (plan 3); the cost roll-up with cache-creation tokens (plan 4); the outline stage is kept untouched. Plans 2 to 4 are reconciled against it. (6) Task 3's file list is completed and the `git mv` order stated (fork S6, fresh S1); `FrameBoxes.seconds`, the engine settings shape and the new record shapes (`Change.records`, `moved` as pairs, `readings` with frames) are declared in D13 (fresh S6). (7) P0 uses three committed config files and a line-count check after each margin run instead of a config edited by pattern; the report takes the margin from the manifest; sheets go to a directory named after the report; the safety check is `find runs/aks -newer`, since `runs/` is git-ignored (fresh S8 and nits). The reading list gains OCR re-splits under changed pixels, which are the known cost of having no groups and are counted, and commands entered and submitted between two emitted frames, which show as appeared text beside a moved prompt and never as appended (fresh S3). The P0 ledger row reports $0 and wall time per frame for `read` and per pair for `track`. (8) Tests: one added, a revert while something else also changes (fresh S5a); the group, pool, clock and `trivial` tests deleted; the pairing tests rewritten for the new rule, one of them on 20-px boxes at a 17-px pitch, the geometry fork S1 read from the stored RapidOCR boxes of frame 178; the three `test_cli_*` smoke tests cut (fresh Q8). Accepted because they remove or clarify: `PairResult.ends` removed; moves only among touched boxes, declared as D22 (fresh Q7); transition kinds `single` and `unsettled` only (D23); `components` left untouched on `decode`'s per-frame path; the `prompts` directory removed so no namespace package survives; `Interpretation` and `HierNode` taken off the forbidden-name test because plan 3 restores them; `test_repo_toml_loads` anchored on the test file; the config-hash test re-homed in `tests/test_config.py`; `gap_ratio` added to the TOML in Task 2; the duplicates fixture renumbered into reading order and the fixture-less tests given fixtures; no cache link when a source has no `cache/`, and the legacy-import message corrected (fork N7, fresh S7); the gray conversion may be read from the tag (fork N6); the report states that the OCR reader's exact rate is a lower bound until `run` links exist (fork N5); a note that the two one-letter answers parse the question's frame (fork N3, second part); sheet crops grown by the rectangle's own height, which needs no margin. **Declined, each because it adds machinery or is minor:** fork S1's two-pass grouping remedy (it repairs a mechanism that was removed instead); a dollar projection for incremental annotation in P0 (tail of fork S3; P0 reports counts and P3 measures dollars); fork N2's overlap-ratio field (a sliver marriage is visible on the sheets; no field until P0 shows the need); fork N3's whole-token-in-a-time-window rule for short entries (more machinery than the stated four-character cut, which stays an evaluation-side choice declared in D17); fork N4's quartiles; fresh B2's moved sheets made by re-running `track_pair` inside the report (the persisted pairs suffice); fresh S5 (b) to (f), five further tests (owner: do not over-weigh tests); fresh S6's moving the `space_guard_fired` counter out of the per-frame engine record (no consumer); fresh Q4's refusal of `--share-cache` for a legacy source (sharing is the tool's purpose and P0 passes `--no-share-cache`); fresh Q8's cuts of the `gap_ratio` config key (parameters live in config by the owner's rule) and of the never-run `frames` parse (plan 4 scores false runs from it). | Both reviewers recomputed every fixture by hand and agreed with the plan's values, disputing only the projection's call count. The owner then ruled (2026-09-21) that reviewers tend to answer minor findings with more machinery, that a faulty mechanism is removed before it is repaired, and that `confusable` and the clock rule go. Grouping existed only to absorb OCR re-splits; the one measurement available puts splits, merges and resizes under one percent of boxes on unchanged pixels (`scratchpad/box-stability/result.txt`), and P0 now counts them under changed pixels, which is the evidence that would justify grouping or show it is not needed. | The first draft of the plan is commit d148751 and both reviews are under `docs/reviews/`; grouping and pools can be restored from there if P0's re-split count calls for them. Two things outside this row's scope, left for the coordinator: the proposal's §5 sketch and §6 steps 6, 7, 10 and 11 still describe groups and the clock rule (§12 governs), and the proposal names the tag `pre-rebase` where the repository has `pre-rebase-boxes`. |
| L46 | 2026-09-21 | **Coordinator's choices on plan 1's reconciliation and on working method while the owner is away.** (1) Pairing is symmetric: candidate pairs are intersecting boxes of which at least one is touched, in either direction, matched one to one greedily by intersection area; read one-sidedly a truncation would have come out as `removed` plus a spurious flicker. One rule, no extra mechanism. (2) A transition's change records are one list (`records`) rather than the proposal's separate groups, appeared and removed lists, so `continues` can name any earlier record; the proposal's §5 and §6 are brought into line with the reconciled plan (groups and the clock rule removed there too; the tag is `pre-rebase-boxes`). (3) A scroll of less than one box height leaves equal texts at overlapping places and produces no change record either way; accepted, P0 will show whether it matters. (4) Command entries under four non-space characters (the `Y` and `y` answers) are reported but not rated: an evaluation-side choice fitted to this list and declared as such. (5) Plans 2 to 4 are reviewed by two fresh read-only reviewers with different lenses (the owner's rulings and simplicity; executability) instead of a fork plus a fresh reviewer: every fork now carries about 850k tokens of conversation, and the owner's rulings are written down in the proposal and in L44 to L46, so a fresh reviewer can hold a plan to them. (6) Git: nothing is pushed (owner); merging and branching are at the coordinator's discretion, erring toward caution and preserved history (owner): no squashing, no deleted branches or tags, no forced updates. Implementation agents commit on `rebase-boxes` task by task; document agents do not commit, the coordinator commits their files. | Each keeps the design smaller or the work moving without the owner. | Each is a line in the plan, the proposal or a brief; none changes recorded data. |
| L47 | 2026-09-21 | **P0, the free first run of the re-base: `read` and `track` over all 221 decoded frames of the sample (`runs/p0`, imported from `runs/aks` by `subset`; code at `f12d105`, reports regenerated at `fc254a1`; `runs/aks` untouched). Finding, no decision: the mechanical layer works end to end, the OCR reader quotes every scorable command of span 2 exactly at every margin, and six rules behaved on real data in ways the plan did not foresee (listed last).** *What ran:* 221 frames, 14,774 boxes (0 dropped empty, spacing guard fired 57 times), 220 transitions (175 single, 45 unsettled), 1,794 lifetimes. *Cost and time:* $0, no model calls; `read` 0.51 s per frame (113.7 s), `track` 0.06 s per pair (13.1 s). *Totals at margin 0.5:* records appeared 1,129, removed 1,129, changed 484, appended 40, truncated 28, reread 15; moved 362 pairs in 43 transitions; same place 611; unchanged 11,992 of which 203 variants; flicker 113 new and 112 lost; reverts 1,249 entries in 82 transitions; textless components 456; 44 transitions have no record. *Guards:* `box_stability` 0.974 (span 2: 0.9753, smoke: 0.9664); `touched_share_low_half` 0.0212 (span 2: 0.0085); `rect_only` 45 boxes (span 2: 7, smoke: 0). *Fragmentation:* 1,794 lifetimes over 1,065 distinct texts, 1.685 per text (span 2: 383 over 276, 1.388; smoke: 255 over 192, 1.328); 100 unstable; 464 seen once. Lifetimes ended by: removed 1,129, changed 484, flicker 112, appended 40, truncated 28. *Incremental-annotation projection:* whole video 221 calls for 221 frames (49 of them without a target), 1,794 target boxes of 14,774 (12.1 %), 8.12 per call against 66.85 boxes per frame; span 2: 33 calls (3 without targets), 383 of 3,972 (9.6 %), 11.61 per call against 120.36; smoke: 11 calls (0 without), 255 of 1,209 (21.1 %), 23.18 per call against 109.91. The saving is boxes per call, not calls. *Commands (span 2, OCR reader, `docs/ground-truth/span2-commands.md`):* exact 7 / 7 scorable entries at margins 0, 0.25, 0.5 and 1.0 alike; first-sighting frame error 0 for six and +1 frame (+0.87 s) for `az aks get-Credentials …` (frame 166 reads `get_Credentials` at the cursor cell); the answers `Y` and `y` are unrated. Never-run suggestions, information only: `az login` has a one-frame lifetime (L1214); the `az aks scale …`, `kubectl rollout undo …` and `kubectl get svc` lines match no lifetime (split over two boxes, or misread at the cursor: `kubect1rollout`). *Margin:* records barely move (appended 40 to 41, changed 478 to 485 from margin 0 to 1.0); what moves is same place (416 to 804) against unchanged (12,199 to 11,792) and textless components (885 to 344). *Re-splits under changed pixels (the cost of no groups):* 7 over both spans: T148/33 and its mirror T154/11 (smoke; a label and its value joined in one frame), T165/0 with T166/0 (one suggestion line in two boxes), T182/8 and T183/3 (toolbar), T185/4; a strict text-containment filter finds 4 in the whole video (T63, T182, T183, T192). *Entered and submitted between two frames:* `kubectl get pods` is `appeared` (T187/4) and `Y` is `appeared` beside the moved bare question `Overwrite? (y/n):` (T170, 169:b85 to 170:b88), as the plan predicted; `kubectl get nodes` came out as `changed` (T180/0) because the line already held `kubectl get` plus a suggestion. *H1:* all 30 unstable-lifetime sheets and all 30 flicker sheets show identical pixels: glyph, spacing, icon and re-split noise, no hidden change (L330 and T141 are the pointer over text). The five largest transitions (T1, T6, T4, T41, T40, 96 to 100 % changed) have no unchanged box at all, so they say nothing; on T148 (25 %, 87 unchanged) and T185 (27 %, 36 unchanged), read box by box against both frames, no box that visibly changed stayed unchanged. *H2:* all 21 appended and truncated records of span 2 pair a box with its own earlier self, and no growing text came out as appeared plus removed; of 25 sampled `changed` records 6 are the earlier self (the edited command line, T157, T167, T173, T179, T180, T184/1) and 19 are unrelated texts married by place when a region is replaced (page navigation T182; also T185/5, T185/9, T216/3 to /11). *H3:* the 12 transitions with ten or more moved pairs are real scrolls, each with one displacement (T76 −212 px checked against the frames); 9 pairs lie off their transition's displacement and about ten small transitions pair equal strings that are different things (T185 `aks-agentpool-18097611-vmss000000`, a portal cell paired with terminal output; T184 `Status`; T142 `Give feedback`; `×`, `V`, `2`). No record is invented; a lifetime hops. *H4:* of the 15 `reread` records 12 are whitespace noise on text that did not change; T162/0, T176/0 and T209/0 are a suggestion accepted or typed through: the same characters, grey then white, which the mechanical layer by contract does not tell apart. A trailing icon glyph or a dropped leading `PS` turns a growth into `changed` (T184/1, T157/0). *H5:* one shape reads well at the small end (54 one-record transitions, about 0.5 kB each) and holds at the large end (T216 144 records, T182 125, T147 113, about 25 kB) with no special mode; what reads badly there is the `changed` kind on unrelated pairs, not the shape. *H6:* `rect_only` is 45 boxes in 18 transitions (most: T182 5; T33, T38, T122, T123 and T218 4 each); the seven looked at (T182 `Status` and `Location`, T33 `API Server` and `Kube-proxy`, T38 `HCL` and `HNS`) sit unchanged inside a hollow component's rectangle, so a rectangle test would have called them touched wrongly. *H7:* for ten of the twenty most repeated texts the lifetime split because the text really left the screen and came back (scroll, tab revisit, a dropdown or window over it: `Tags`, `Review + create`, `Networking`, `sub-global`, `AKS1-KodeKloudApp`), because several instances are on screen at once (`Create \| Learn more` 9, `V` 5, `Node pools` 3), because a new bare prompt is a new text each time (`PS C:\Users\msadmin>` 13, 7 ended by `appended`), or because an icon glyph is detected intermittently (`V` 13 of 23 ended as flicker). None split on unchanged pixels for an OCR reason other than flicker. *H8:* the frame-151 tooltip is found (T152 reverts T151, two components, held 0.6 s); the frame-149 tooltip is NOT (T150 has no revert: the pointer that raised it left with it, and 109 of the component's 799 pixels, all under the pointer's hand and shadow, still differ between frames 148 and 150). Reverts are per component, so one visual undo is many entries: 252 of 1,249 have a rectangle under 100 px², T96 has 205 (a scroll there and back), T184 has 103 (blank, then page content, then another page blank at the same places), T166 and T179 are a suggestion's tail shown and replaced, T153 and T154 are the pointer passing. *Watch-list cases that occurred:* the pointer clipping a box (T149/0 reads `oud Shell`; L330 `(US) West WS`; T141 `m` read from the pointer); the cursor cell misread (T166/0 `get_Credentials`, T172/0 `kubect1rollout`, T173/0 `kubectl_rollout`, T178/0); an occluding window edge (T177/0 `Standard_DS2_`, T185/1 to /4 `↑.1. Protection nolicv`); occlusion by the video's animated SUBSCRIBE overlay (T184/20 to /22, T185/42 to /45, T186/2 to /5; T13, T15); duplicate strings under a scroll (T202, T213); a greyed-out button detected intermittently (`Delete`, T185, T187, T203). Not seen: re-wrap on resize, a text change under θpix. | The plan's Task 16; every number is from `runs/p0/report.md`, `report-smoke.md`, `report-span2.md` and the three `runs/p0-m*/report.md`, and every reading from the evidence sheets under `runs/p0/report*-sheets/` and the frames. **Rules the real data showed to be wrong or undefined, as findings and not fixes:** (1) Task 12 rule 4: a revert is missed when the pointer leaves with the tooltip (T150), is counted once per component, and fires on blank-content-blank and on a scroll there and back. (2) Task 9 rule 3: equal strings that are different things are paired as moves when one window covers another. (3) Task 10 rule 1: with no floor on the overlap and no regard to text, a replaced region yields `changed` records between unrelated boxes, whose character diffs are noise. (4) Task 10 rule 3: a `reread` is not always noise, and an icon glyph or a dropped prefix defeats the prefix test. (5) Task 8 rule 3: a re-split on unchanged pixels becomes variants of different extent in one lifetime (L1397 `Start Restart Stop  Reimage Delete ↑ Upgrade Refresh` / `↑ Upgrade`; L1211; L1560), whose majority text may be a fragment, and the other parts become flicker. (6) Exact equality for moves leaves an OCR-variant line under a scroll as removed plus appeared or as a cross `changed` (T187/0, `Standard ($$)` in T76). | Nothing here changes code or recorded data; `runs/p0*` are git-ignored and can be regenerated free with the plan's Task 16 commands. Each finding is a question for the owner under §12 (simplify before repairing): whether to remove the mechanism, accept the behaviour, or add logic on this evidence. |
| L51 | 2026-09-21 | **What is done about P0's six unforeseen behaviours (L47): one simplification, one wording choice, four left alone.** (1) Reverts are recorded ONCE PER TRANSITION, not once per changed component: the share of the previous transition's changed pixels that are back to what they were before it, with the hold time. P0 produced 1,249 per-component entries in 82 transitions (205 in one), which nothing downstream can read; one number per transition is smaller, has no threshold, and still says "most of what appeared has gone again" for a tooltip whose pointer also moved (the frame-149 case the per-component rule missed). (2) A `changed` record pairs what was at a place with what is there now; on a page navigation that marries unrelated texts (19 of 25 sampled). The record is honest as it stands, so the mechanism stays and the text given to `interpret` says "at this place X was replaced by Y" instead of implying an edit; no similarity threshold is added. (3) Left alone, as minor and honest: equal strings that are different things paired as a move when one window covers another (a lifetime hops, no record is invented); kind labels defeated by an icon glyph or a dropped prompt prefix (labels are conveniences, the strings are the record); re-splits on unchanged pixels becoming variants (7 in both spans); OCR-variant lines under a scroll appearing as removed plus appeared. None is worth machinery on the evidence of one video. (4) Carried forward as evidence: OCR's majority reading quotes all 7 scorable commands of span 2 exactly at every margin, so the margin stays at half a box height; incremental annotation would label 12.1 % of boxes (8.1 per call against 66.9 per frame), so its saving is output tokens per call; `read` costs 0.51 s per frame and `track` 0.06 s per pair. | The owner's rule: simplify before repairing; add nothing that is minor or fitted to one video. | The revert summary is one function in `track`; the wording is one line of the change-text renderer. |
| L48 | 2026-09-21 | **Build plan 3 (`interpret`, `summarize`, `index`, `ask`) reconciled in place with its two read-only reviews (`docs/reviews/2026-09-21-rebase-plan3-review-rulings.md`, `…-executability.md`), both "accept with changes", under the mandate of L44. The rule applied was the proposal's §12, simplify before repairing; the plan went from sixteen tasks to ten and from about 93 named tests to 66.** The coordinator's thirteen decisions, each applied: (1) **Records.** Fixture M and every task that touches change records or lifetimes are rewritten to the shapes committed in `src/scry/schemas.py`: one list of per-box change records (kinds reread, appended, truncated, changed, appeared, removed), `moved` as (earlier id, later id) pairs, a lifetime's readings mapping each reading to its frames, no `Group`, no `clock`; the executability review's mapping table was used. The change text given to `interpret` is ONE LINE PER CHANGE RECORD in record order with its citable box ids (one list, not three headings, so `continues` reads naturally for any kind); the one-liners, summary items and transition entries read pair records and count `appeared`, `removed` and `len(moved)`; lifetime entries, their payloads and the frame tool count a reading by the length of its frame list. Every expected string and number the change affects was recomputed by hand (listed in the plan's self-review); Fixture M's `touched_share` for T2 was corrected to 2 of 8 boxes. Followed as well, because the committed code is the authority and it moved after the reviews were written (L51): a transition has at most one revert, rendered "Undoes 93% of T4's change after 3.4 s." with no rectangle, and a `changed` record is rendered "at this place X was replaced by Y", with one sentence in the `interpret` prompt saying the two texts may be unrelated. (2) **`trivial` deleted everywhere** (rules, stats, tests, D21): every transition is interpreted, summarised and indexed; the empty case is an empty `changes.jsonl`. (3) **Suite green at every commit:** the three existing tests the plan makes false are named in the task that first does so: `tests/test_package.py::GONE_MODULES` loses `interpret` (and `prompts` if plan 2 has not) in the task that re-creates the module; the `node()` helper of `tests/test_index.py` changes with `Node`; plan 2's `test_stage_list` is deleted in the task that wires `run`, where `test_run_executes_stages_in_pipeline_order` pins the list instead. (4) **The `non_text` skip on lifetime entries is removed** (D11): every lifetime is indexed and the flag is payload only; a stat, a test and an unstated interaction with link anchors go, F6 becomes unconditional, and the three bases index one set of lifetimes. This departs from the letter of the proposal's §5 ("leaves … index text") in the direction of the owner's later ruling. (5) **`cost_usd_batch` and `stage_cost` removed:** the batch figure was always exactly half of `cost_usd`; one figure per stage and per question, a report halves it. The cost task is folded into the task that wires `run` and keeps only `run_costs`; pricing of cache-creation tokens at 1.25 × and usage summed over retries land with plan 2's provider task and are not duplicated. `scry run` says that the figure is what a cold run would pay. (6) **Image modes for `interpret` are `scaled` (default, L39) and `full` only.** The `text` mode is dropped for good: L39 measured it fabricating a click, misplacing the cursor and explaining a tooltip as an OCR artifact at confidence 0.57 against 0.84, and a mode without frames contradicts principle 4. The `crops` mode is DEFERRED to P5, the phase that needs it, and is not built now (it is also no longer the mode L39 measured, so P5 prices it afresh); deferred likewise the `[ask] prompt` registry (until a second wording exists) and the paging of the transitions tool (until a real question is cut). A "Deferred" section names each, where it returns and that the first draft (commit `4da46ac`) holds its rules and hand-checked values. With two modes there is no fallback, so `Interpretation.images` and the `image_fallbacks` stat go too. (7) **Findability.** Pane names are indexed in the whole-screen frame entry whenever a pane label exists (one line per distinct pane, after the container lines); `ask`'s search tool ignores its application filter, and says so in the result, when the run has no annotations (new invariant F8, one test); the guarantee section is reworded where it overclaimed (F2: unquoted terms and phrases inside one box; F4: the substring matcher can see FEWER lines than the tokenizers, which loses nothing because both frames stay members and each string has its lifetime entry) and no matcher is added; the four-family rank fusion stays, with its real reason (BM25's length normalisation ranks one-line lifetime entries above whole-screen entries, so in one pool a frame entry loses its place under `LIMIT k`) and its accepted consequence (a round-robin across families; no weights, no per-family k). A short "Known losses" section states the one loss that remains: a quoted phrase spanning two OCR boxes matches only when the boxes are adjacent in reading order in the frame entry or joined by a link, widest in the no-annotation base; it is to be reported to the owner with the first paid phase's results, question by question. (8) **`ask`:** the top-level `cache_control` request field is removed (never exercised; the tag's agent did not cache; its first exercise would have been the paid question set); one rule for an exception raised by the API call: stop, return the error in the result (`stop "api_error"`), never crash the run. Citations stay as they were: `interpret`'s are validated against the handed ids, `summarize`'s refs against the segment's children, and `ask`'s are read from the prose and validate nothing, which the plan now says. (9) **Metrics: plan 3 defines no metric and no scorer.** Found, exact, the time errors and false run are plan 4's; plan 3 guarantees only the data (A9, D20): `entered_text` (the whole submitted text on the submitting transition) and `submitted`, per-interpretation `usage` and `invalid_citations`, and the search hit shape with `frames` and `t` covering every collapsed member. (10) **Small fixes:** links are rendered by dispatching on `kind` and reading that kind's fields only (plan 2's per-kind classes); hand-written annotation fixtures carry `targets`, `model` and `prompt_version`; `propagate` decides by type, so frame 0 is a frame (its test now starts at frame 0); `interpret` lists `outline.json` among its inputs; "after plan 2's Tasks 1 to 11"; specification revision 3.1. (11) **Shared conventions:** stage order `decode, outline, read, track, annotate, interpret, summarize, index`; helpers in `tests/fakes.py` and per-plan fixture modules, imported as top-level modules with `pythonpath = ["tests"]`, added by whichever plan lands first. (12) **Tests:** cut, as beyond the rules: five CLI smoke tests, the three tests of test helpers and defaults, `test_old_stage_keys_are_rejected`, `test_matched_lines`, `test_collapse_runs_need_consecutive_ordinals`, `test_blank_frame_has_no_node`, `test_state_lines_use_containers_when_labelled`, and the tests that left with `trivial`, the `non_text` skip, `stage_cost`, crops (four), the prompt registry and paging; the three prompt-wording tests became one that checks only the rulings (no focus, nothing of the sample, `submitted` present), so that P5's wordings need not be written around a test; five small tests were folded into neighbours. Kept: all eight findability tests, both end-to-end tests, every test that pins a rule, a record shape, a ranking or a collapsing outcome. (13) **Accepted because they remove or clarify:** the second fake provider (`ScriptedProvider`) is gone, plan 2's `AnswerProvider` serves every stage test and answers by the call's text, never by arrival order; plan 2's `frame_block` is shared instead of a second in-memory PNG helper; the first draft's wish for box refs in `same_place` (A8) is deleted; three echoes of the sample video leave the prompts (the boundary example's portal scenario, a time inside span 2, "l and 1"); the `ask` prompt no longer lets several sightings license a verbatim quote (on unchanged pixels they are one observation) and describes windows, description and second reader as present only when the record has them; the anchor rule's reason is recorded (D12); an unknown `level` returns `[]`; the frame query names where `ordinal` comes from; ties sort on the rounded score; a query SQLite rejects is an empty ranking; `ask` and `scry search` refuse a run without `index.sqlite` instead of creating one; the video's elaboration is `Segment V`; an errored annotation record is covered inside an existing findability test. **Declined, each because it adds machinery, is minor, or is superseded:** executability S6's `[ask]` boolean to switch `cache_control` off (the field is removed instead); executability S8's repair of the paging keys (the paging is deferred, so it dissolves); rulings N3, letting a missing frame PNG raise (`track` degrades and `annotate` records the same error, and a raise inside a concurrent stage discards every other record); rulings N7 and the second half of executability B3(b), keeping `outline` first in `STAGES` (superseded by the order of item 11); the first half of rulings N8, renaming `run_costs` (plan 4 already consumes it by that name; its own `run_cost` is plan 4's to rename); config keys for `summarize`'s 80, 60 and 25 (rulings N4 asked to pick one: they stay the tag's literals, named, until a run needs to vary them; the windowing has never run on real data); the two-pool alternative to four families (rulings, coordinator question 6: long frame entries would sink under short transition entries); a matcher faithful to the tokenizers and any row-banding rule for the phrase loss (both reviews agree none should be added); tests for the executability review's list of rules without a test (owner: do not over-weigh tests); adding `lifetimes.jsonl` to the inputs of `interpret` and `summarize` (no effect in practice: `track` writes it with `changes.jsonl`); a retry path for uncached API-error records in a finished stage (inherited from the tag; no run has shown the need, the manifest's `errors` stat shows the hole). | The owner's rulings as written in the proposal and L44 to L46: remove before repairing, add nothing pre-emptively, nothing findable today becomes unfindable and nothing is dropped without a stated reason, do not over-weigh tests. Both reviewers recomputed the plan's arithmetic by hand and disputed none of its numbers; their blocking findings were all stale interfaces against plan 1's simplified records, which made the plan smaller. | The first draft is commit `4da46ac` and both reviews are under `docs/reviews/`; every deferred or removed piece (crops, the prompt registry, paging, the `non_text` skip, `cost_usd_batch`, `cache_control`) can be restored from there, each as one small task. Left for the coordinator, outside this row's two files: plan 1's "Returns with" table still says the `text` and `crops` modes and the cost roll-up return as first planned; the proposal's §5 sentences on `entered_text` (D2), on `non_text` leaving index text (D11) and on per-component `reverts` (L51) are now behind the plans; plan 4's text on disk still expects three scorers "from plan 3's Task 16", `cost_usd_batch`, `images = "crops"`, `[ask] prompt` and `AskResult.answer`, all answered in plan 3's A9; plan 2 must keep `AnswerProvider` (async), `frame_block`, `BoxLabel.pane` and the shared stage order, or A4 and A6 of plan 3 are renames. |
